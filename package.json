{
  "name": "code-execution-sandbox",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "cleanup": "node -e \"const { cleanupTempFiles } = require('./codeExecution'); cleanupTempFiles();\""
  },
  "keywords": ["code-execution", "sandbox", "docker", "nodejs", "react"],
  "author": "",
  "license": "ISC",
  "description": "A secure code execution sandbox supporting multiple programming languages",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
